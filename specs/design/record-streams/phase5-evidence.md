---
id: RECORD-STREAMS-PHASE5-EVIDENCE
kind: analysis
title: Phase 5 evidence log — record streams
workflow: liquers-project
status: draft
area: [lib/value, core/assets, core/recipes]
created: 2026-09-25
---
# Phase 5 evidence log — Record streams

Kept while Phase 4 is executed ([plan](./phase4-implementation.md) §"Phase 5 evidence capture"),
one entry per step: requested versus implemented scope, tests corrected and why, issues filed,
surprises, measurements. Phase 5 synthesizes from this log rather than rediscovering it.

| Step | Commit | Scope as planned? | Tests | Corrections, issues, surprises |
|---|---|---|---|---|
| 0.1 | (this commit) | Yes, plus the `f32`/`u32`/`u8` `TryFrom` impls through checked narrowing (out of range → error, never truncation) | 14 new in `extended.rs`; 17 pass in the module | **Toolchain:** the container had rustc 1.94.1, and default-feature `liquers-lib` builds refuse it (`egui` 0.36 and `sysinfo` 0.39 need 1.95 — `BUILD-SYSINFO-REQUIRES-NEWER-RUSTC`). Updated to stable 1.98.1, as CI uses; `cargo clean` freed 4 GB of stale artifacts. **Plan wording:** issues close as `status: closed`, not `complete` (a design-only status) — corrected in the plan. Pre-existing `_ =>` arms on `try_into_key`/`try_into_bytes` left alone (out of scope) |
| 0.2 | (this commit) | Yes, and wider: JSON reads consult the type identifier (structured variants as their own types; `Array`/`Object`/`Bytes`/`Query`/`Key` in the tagged form `as_bytes` writes, else plain JSON) | 20 per-format tests from the agent, plus the TypeInfo-driven `every_declared_format_round_trips_or_is_recorded_as_unwritable` written by the orchestrator | **Test corrected:** the agent's array/object round trips built their bytes by hand rather than through `as_bytes`, so they could not see that the writer emits a tagged form the plain reader does not accept; the TypeInfo-driven test found it. **Issue filed:** `SIMPLE-VALUE-WRITES-FEWER-FORMATS-THAN-DECLARED` (42 declared pairs the writer refuses, recorded in the test). **Pre-existing red:** `registry_export` failed on a clean tree — every argument's default `TextField` width had changed 20 → 40 without a re-export; the registry was regenerated with a changelog line. `SimpleValue` gains `PartialEq` (for assertions) |
| 1.1 | (this commit) | Yes | Phase 3 §4.2's 10 tests, in `recipes.rs`, unchanged | None. `liquers-py`, `liquers-store`, `liquers-axum` compile unchanged, as decision 6 expected |
| 1.2 | (this commit) | Yes. Seeding differs from the plan's wording: the flags are set on the ad-hoc `key.into()` recipe **before** the asset is built, so `Recipe::get_asset_info` (Step 1.1) carries them into the first `MetadataRecord` — no separate metadata patch | `stored_cached_flags.rs`: 6 scenarios × both managers = 12 tests. Efficacy checked: with `assets.rs` reverted, the 6 that exercise the fix fail | **Issue filed:** `DEFAULT-ASSET-MANAGER-RECIPE-OPT-SKIPS-PAYLOAD-CHECK` — the two managers reject a payload-requiring keyed recipe at different points (pre-existing). `cached: false` skips registration outright (`entry_async`/`map.insert`) rather than registering and removing |
| 1.3 | (this commit) | Yes | Phase 3 §4.1's 5 tests unchanged, plus `appended_provider_is_consulted_after_the_configured_one` | **Semantics refined:** the chain's `contains` is "any provider whose `recipe_opt` answers `Some`", not "any provider's `contains`" — a provider's own `contains` may rest on the enumerating default, and Phase 3's fixture shows it; Phase 2's §B table corrected. `recipe` / `recipe_plan` / `get_asset_info` delegate whole to the provider that has the key. **Milestone 1 complete** at this commit (local tag `record-streams-m1`; tags cannot be pushed here) |
| 2.1 | (this commit) | Yes. `Bitmap::is_empty` added (clippy's `len_without_is_empty`); `Buffer<T>`'s traits hand-written so the bound stays `T: Pod` | §2.2's 14 unchanged, plus 6 (serde round trips, copy-on-write `set`, padding bits) | **Plan corrected:** `bytemuck` needs its `derive` feature for `#[derive(Pod, Zeroable)]` on the aligned chunk — the design's "no `unsafe`" depends on it (plan and Phase 2 Cargo blocks fixed). Alignment: `Arc<[AlignedChunk]>` of `#[repr(C, align(64))] [u8; 64]`, cast with `bytemuck::cast_slice`; **no `unsafe` in the crate** |
| 2.2 | (this commit) | Yes, plus `RecordSchema::resolve_field` (qualified-name resolution had prose in Phase 2 but no signature) and a cached `text_fields` index; `RecordSchema` deserializes through `new` (`try_from` a private spec) so a hand-written schema is validated too; `FieldSchema`'s `label` defaults from `name` on deserialization | §2.1's 13; one corrected | **Test corrected:** `schema_new_id_with_contradicting_role_refused` used `FieldRole::ignored()`, which equals `FieldRole::default()` — and Phase 2 *supplies* the Id role when it is left default — so it now uses `numeric()`, a genuinely contradicting role. `FieldRole` constructor bodies chosen (Phase 2 names them only): `text` = FullText(simple, positions), `keyword` = Exact, `numeric` = Range, `vector` = Similarity, `stored_only`, `ignored` = default |
| 2.3 | (this commit) | Yes, split into `column.rs` (values, columns, kernels) and `batch.rs` (ids, origins, batch, the view and stream traits). `Buffer<T>` gains serde as a `Vec<T>` (readable). `RecordView::is_empty` added. `ChunkOrigin::locator_query` deferred to Step 4.2 (no caller yet; plan updated) | §2.3's 11, plus ~35 more; 82 in the crate | **Test corrected:** a `_ =>` arm on `FieldValue` became `matches!` (CLAUDE.md). **Issues filed:** `METADATA-LACKS-SERIALIZE-DESERIALIZE-AND-PARTIALEQ` (Phase 2 derives `Serialize`/`PartialEq` on `ChunkDescriptor`, which holds a `Metadata` that has neither — `ChunkDescriptor` is `Debug, Clone` for now); `COLUMN-SLICE-COPIES-INSTEAD-OF-SHARING-BUFFER-STORAGE` (buffers carry no offset, so `slice` copies where Phase 2 says zero-copy). `query_format` serde helper is private in core, so a copy lives in `batch.rs`. New docs area `records` (DOCS_STRUCTURE_GUIDE + `docs_index.py`) |
| 2.4 | (this commit) | Yes. Constructors on `impl dyn RecordView`: `select_columns`, `slice`, `row`, `cell`, `filter`, `take`, `with_column`, `with_columns`, and `single_cell` (the scalar reading; Phase 2 names no helper). `RowIndexView` overrides `materialize` to keep one exact `RowRun` per row | §2.4's 11 and §2.5's 4, plus ~20; 120 in the crate | **Tests corrected:** the constructors take `self: &Arc<dyn RecordView>`, so `Arc<RecordBatch>` bindings needed a `: Arc<dyn RecordView>` annotation (E0599 otherwise); one closure used `ColumnMut` (Step 2.5) and now builds the column directly. **Fixed rather than filed:** `RecordSchema::new` accepted duplicate field names, which a derived column could shadow — now refused (test added). Resolutions: forced `Id`/`Source` columns go after the named ones; `cell` = `row` then `select_columns`; `RowIndexView::column_range` reads only the span of selected rows |
| 2.5 | (this commit) | Yes: `RecordViewMut` (`reserve`, `append_row`, `set_value`, `column_mut`), `RecordBatchMut`, `ColumnMut` (`push`, `set`, `freeze`), `RecordBatch::into_mut`. `RecordBatchMut::len` is computed (shortest column), not cached | §2.6's 8 unchanged, plus 12; 140 in the crate | **Zero-copy takeover is real:** `AlignedBuffer`'s storage became `Arc<Vec<AlignedChunk>>` (internal) so `Arc::try_unwrap` applies, with a crate-private growable `AlignedBytesMut`; an unshared batch's value buffers move without copying, a shared one's are copied — both tested. **Issue filed:** `COLUMNMUT-VALIDITY-AND-VARIABLE-LENGTH-SET-ALWAYS-COPY` (validity masks always copy; `set` on text/binary rebuilds the column) |
| 2.6 | (this commit) | Yes: `value.rs` holds `RecordSource`, `ChunkResolver`, `ChunkValue`, `RecordValue`; `manifest.rs` holds only the `ManifestSpec` / `ManifestKind` / `ChunkTemplate` declarations and `ManifestSpec`'s hand-written `Default` | 8 new (object safety, `ChunkValue`, manifest default and discriminator, `materialize` uniform / non-uniform / over-limit); 149 in the crate | `DEFAULT_MATERIALIZE_MAX_ROWS = 1_000_000` is a constant, since a trait method has no default arguments — the command's default uses it. `RecordSource::materialize`'s refusal of a non-uniform source falls out of `RecordBatch::concat`. `RecordValue` is untested here by design (no honest implementor without the lib `Value`). **Milestone 2 complete** at this commit (local tag `record-streams-m2`) |
| 3.1 | (this commit) | Yes: `TableFormat` (all variants exist; unimplemented ones return a typed "not supported" error through explicit arms), `ReadSchema`, the options, `read_table`/`write_table`, inference, CSV/TSV both modes; base64 for `Binary` and a JSON array for `Vector` hand-written (no new dependency) | §3.1's 13 (3 corrected) and §3.6's 3, plus 23; 189 in the crate | **Tests corrected:** three used `String::from_utf8(bytes)?` where core's `Error` has no `From<FromUtf8Error>` — a local helper maps it explicitly. **Resolutions:** the canonical round-trip rule applies to `Float` as to `Int` (a `+5` or `1e3` column stays `Text`, as §3.1 requires); inferred `nullable` is true only when a null was seen. **Follow-on fix (orchestrator):** that made two inferred chunks disagree on nullability, and `RecordBatch::concat` kept the first schema — it now widens a field to nullable when any input is (test added) |
| 3.2 | (this commit) | Yes: NDJSON and the `json` format (both reader modes), `JsonOrient` with `to_json`/`from_json` over all eight orients; every orient funnels through the NDJSON object reader, reusing CSV's rendering of dates, timestamps and binary | §3.2's 3 and §3.3's 12 (the latter corrected), plus ~27; 233 in the crate | **Tests corrected:** §3.3 used `serde_json::from_str(..)?` where core's `Error` has no `From<serde_json::Error>` (local helper); a §3.6 test asserted `Json` unsupported — now `Markdown`. **Fixed (orchestrator):** the `table` orient dropped `nullable: false` (now Frictionless `constraints.required`); `columns`/`index` read rows in string key order (`"10"` before `"2"`) — integer keys now sort numerically. **Issue filed:** `SCHEMA-LESS-JSON-READS-SORT-COLUMNS-ALPHABETICALLY` (`serde_json` without `preserve_order`). `auto`: array of objects → records, of arrays → values; object with schema+data → table, columns+data → split, equal-length arrays → list; object of objects refused as the columns/index ambiguity |
| 3.3 | (this commit) | Yes: Markdown write and read (a GFM table, headers from labels, types inferred), HTML write-only with full escaping. Plan wording corrected — it called both write-only; Phase 2's format table and §7 read Markdown back | §3.4's 3, §3.5's 4, §7's 8 (6 run by default; the IPC and Parquet round trips are feature-gated and pass once Milestone 6 implements those formats) | `String::from_utf8(..)?` corrected as in 3.1. **Milestone 3 complete** at this commit (local tag `record-streams-m3`) |
| 4.1 | (this commit) | Yes: `ChunkTemplate::{query_at, offset_at}` (the query is parsed, so a malformed template fails at load), `ChunkNaming::{key, index_of}` (canonical names only), and `ManifestSpec::check_explicit_chunk_collisions` / `check_explicit_names_against_template` for `ManifestSource` to call | 8 of §2.7 and 4 of §9 (the 5 that build a `ManifestSource` wait for 4.2), plus 5 | **Correction reversed (orchestrator):** the agent changed a test's `version: "1.5"` to `15` because `version` is now `Option<u32>` — but `manifest-format.md` §8 promises an unknown version reads as the latest. `version` now deserializes leniently (any non-whole-number scalar → `None`) and the test keeps `"1.5"` |
| 4.2 | (this commit) | Yes: `ManifestSource` (`new`, `with_key`, `spec`, `key`, serde through `ManifestSpec`; `stream` walks explicit then template chunks one per poll through `unfold`, ending at a short template chunk), `InMemorySource`, `ContextResolver` / `EnvResolver` (compile-checked; behaviour in 5.6), `ChunkOrigin::locator_query`, and `ManifestSpec::check_unkeyed_chunk_arguments` | 5 deferred from 4.1, `manifest_spec_serializes_its_discriminator`, §5.6's 3, §5.7's 1, §6's 9 native, plus ~20; 282 lib + 19 integration | **Keyed chunk read:** "the store holds the key" is decided at run time — `read_resource`, falling back to evaluation on `KeyNotFound` — so an existing copy is preferred even under `stored: false`. **Tests corrected:** a fixture query `select 1` (not a query); `Query` has no `FromStr`; `Arc<RecordBatch>` needed `as Arc<dyn RecordView>`; a use-after-move of `source`. **Orchestrator fixes:** the uniform-schema check compared nullability *flags* exactly — now a field declared not null must simply hold no nulls; the `ipc`-gated round trip named a nonexistent `TableFormat::Feather` (now `Ipc`; the agent's issue for it withdrawn). **Issue filed:** `RECORD-SOURCE-WRAPPERS-UNSPECIFIED` (Phase 2 names a filtering and an async-mapping source without specifying either) |
| 4.3 | (this commit) | Yes: `ManifestRecipeProvider` for any `E` — a template-shaped name checks only `<folder>/<prefix>.manifest.yaml`; explicit names scan the folder's manifests from a cached listing; manifests cached by key and store version (content hash when the store gives none); the `recipes.yaml` collision check runs only when a match is about to be returned. `ManifestSource::naming()` added | 12 in `provider.rs`; 293 lib + 19 integration | **Issue filed:** `MANIFEST-PROVIDER-FOLDER-LISTING-NEVER-REFRESHES` (raised to P2 by the orchestrator — a manifest added to a running server's folder silently serves no explicit chunks). `RECIPE-CONTAINS-DEFAULT-ASSUMES-ENUMERABILITY` noted as resolved for this provider, open at trait level. `recipe_plan` uses `Recipe::to_plan_for_key` (core's fuller helper is `pub(crate)`). **Milestone 4 complete** at this commit (local tag `record-streams-m4`) |
